package io.github.marcsanzdev.chestseparators;

import static org.junit.jupiter.api.Assertions.assertEquals;

import org.junit.jupiter.api.Test;

/**
 * Scaffolding test that proves the JUnit 5 harness is wired up correctly.
 * Real coverage of the data and utility layers is added in a later phase.
 */
class HarnessSmokeTest {

    @Test
    void junitHarnessIsAvailable() {
        assertEquals(4, 2 + 2);
    }
}
