org.gradle.jvmargs=-Xmx3G
org.gradle.parallel=true

minecraft_version=1.21.11
enabled_platforms=fabric

archives_base_name=absolute-order
mod_version=2.0.0
maven_group=io.github.marcsanzdev

# Loader / API versions taken from the working single-loader build (known-good for 1.21.11).
fabric_loader_version=0.19.3
fabric_api_version=0.141.2+1.21.11

# architectury_version and neoforge_version are added when we wire the Architectury API and the
# NeoForge module (next skeleton steps).
